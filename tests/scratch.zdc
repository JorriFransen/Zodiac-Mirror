
_c :: import c;
using _c;

main :: ()
{
    open_std_handles();

    poly_test(1);

    return 0;
}

poly_test :: (value: $T)
{
    for (i := 0; i < 1; i++)
    {
        printf("value: %d\n", value);
    }

    while (true)
    {
        printf("value: %d\n", value);
        break;
    }

}

