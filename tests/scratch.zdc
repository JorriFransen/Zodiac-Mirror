
_c :: import c;
using _c;

main :: ()
{
    poly_test(1);
}

poly_test :: (value: $T)
{
    open_std_handles();

    for (i := 0; i < 1; i++)
    {
        printf("value: %d\n", value);
    }

    while (true)
    {
        printf("value: %d\n", value);
        break;
    }
}

