
_c :: import c;
using _c;

main :: ()
{
    open_std_handles();

    printf("scratch\n");

    int_arr : [4]int = { 1, 2, 3, 4 };
    // assert(int_arr[0] == 1);
    // assert(int_arr[1] == 2);
    // assert(int_arr[2] == 3);
    // assert(int_arr[3] == 4);

    print_arr(int_arr);

    return 0;
}

print_arr :: (arr: [4]int)
{
    printf("{ ");

    for (i := 0; i < array_length(arr); i++)
    {
        if (i > 0) printf(", ");
        printf("%d", arr[i]);
    }

    printf(" }\n");
}

