
c :: import c;

main :: () -> void
{
    ch := c.getchar();
    if (ch == 49)
    {
       c.printf("You choose number one\n");
    }
    else if (ch == 50)
    {
       c.printf("You choose number two\n");
    }
    else if (ch == 51)
    {
        c.printf("You choose number three\n");
    }
    else if (ch == 52)
    {
        c.printf("you choose number four\n");
    }
    else
    {
        c.printf("Invalid option\n");
    }
}
