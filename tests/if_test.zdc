
_c :: import c;
using _c;

main :: () -> void
{
    if (true)
    {
        printf("if level one\n");
        if (true)
        {
            printf("if level two\n");
        }
    }
    else
    {
        if (false)
        {
            printf("else level one, if level two\n");
        }
        printf("else level one\n");
    }

    ch := getchar();
    if (ch == 49)
    {
        printf("You choose number one\n");
    }
    else if (ch == 50)
    {
        printf("You choose number two\n");
    }
    else if (ch == 51)
    {
        printf("You choose number three\n");
    }
    else if (ch == 52)
    {
        printf("you choose number four\n");
    }
    else
    {
        printf("Invalid option\n");
    }
}
